/account/expense/
  POST, returns a json
  input; date(optional),text, amount,user
  output: status:OK

  /account/income/
    POST, returns a json
    input; date(optional),text, amount,user
    output: status:OK
